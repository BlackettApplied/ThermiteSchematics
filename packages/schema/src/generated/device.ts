/**
 * Generated from the canonical Thermite Schematics JSON Schemas.
 * Do not edit this file directly; run npm run generate:types.
 */

/**
 * Alternative names for an entity.
 */
export type Aliases = string[];

export interface Device {
  /**
   * Lowercase canonical RFC 4122/9562 UUID using version 1 through 8 and the RFC variant.
   */
  uid: string;
  kind: "device";
  /**
   * Opaque non-empty identifier with no control characters or surrounding whitespace.
   */
  designation: string;
  /**
   * A library-qualified type identifier in <library-name>:<type-name> form.
   */
  type: string;
  description?: string;
  aliases?: Aliases;
  location?: string;
  io?: {
    /**
     * Opaque non-empty identifier with no control characters or surrounding whitespace.
     */
    addressSpace: string;
    channels: {
      /**
       * This interface was referenced by `undefined`'s JSON-Schema definition
       * via the `patternProperty` "^.+$".
       */
      [k: string]: {
        /**
         * Opaque non-empty identifier with no control characters or surrounding whitespace.
         */
        address?: string;
        /**
         * Opaque non-empty identifier with no control characters or surrounding whitespace.
         */
        signal?: string;
        usage?: "in-use" | "spare";
      };
    };
  };
  connectionReview?: {
    terminals?: {
      [k: string]: ConnectionReviewEntry;
    };
    ports?: {
      [k: string]: ConnectionReviewEntry;
    };
    connectorPorts?: {
      [k: string]: ConnectionReviewEntry;
    };
  };
}
/**
 * This interface was referenced by `undefined`'s JSON-Schema definition
 * via the `patternProperty` "^.+$".
 *
 * This interface was referenced by `undefined`'s JSON-Schema definition
 * via the `patternProperty` "^.+$".
 *
 * This interface was referenced by `undefined`'s JSON-Schema definition
 * via the `patternProperty` "^.+$".
 *
 * This interface was referenced by `Device`'s JSON-Schema
 * via the `definition` "connectionReviewEntry".
 */
export interface ConnectionReviewEntry {
  status: "required" | "intentionally-unused" | "deferred";
  reason: string;
}
