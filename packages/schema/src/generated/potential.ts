/**
 * Generated from the canonical Thermite Schematics JSON Schemas.
 * Do not edit this file directly; run bun run generate:types.
 */

/**
 * Alternative names for an entity.
 */
export type Aliases = string[];

export interface Potential {
  /**
   * Lowercase canonical RFC 4122/9562 UUID using version 1 through 8 and the RFC variant.
   */
  uid: string;
  kind: "potential";
  /**
   * Opaque non-empty identifier with no control characters or surrounding whitespace.
   */
  designation?: string;
  description?: string;
  aliases?: Aliases;
  /**
   * Opaque non-empty identifier with no control characters or surrounding whitespace.
   */
  name: string;
  at: TerminalRef;
  electrical: ElectricalIntent;
}
export interface TerminalRef {
  /**
   * Opaque non-empty identifier with no control characters or surrounding whitespace.
   */
  device: string;
  /**
   * Opaque non-empty identifier with no control characters or surrounding whitespace.
   */
  terminal: string;
}
/**
 * Declared electrical intent. This is distinct from equipment capability or rating.
 */
export interface ElectricalIntent {
  /**
   * Nominal voltage in volts (V), the canonical source unit.
   */
  nominal_voltage?: number;
  voltage_type?: "AC" | "DC";
  /**
   * Opaque polarity classification, such as positive, negative, or neutral.
   */
  polarity?: string;
  /**
   * Current in amperes (A), the canonical source unit.
   */
  current?: number;
  /**
   * Power in watts (W), the canonical source unit.
   */
  power?: number;
  /**
   * Frequency in hertz (Hz), the canonical source unit.
   */
  frequency?: number;
}
