/**
 * Generated from the canonical Thermite Schematics JSON Schemas.
 * Do not edit this file directly; run npm run generate:types.
 */

/**
 * Alternative names for an entity.
 */
export type Aliases = string[];

export interface Relation {
  /**
   * Lowercase canonical RFC 4122/9562 UUID using version 1 through 8 and the RFC variant.
   */
  uid: string;
  kind: "relation";
  /**
   * Opaque non-empty identifier with no control characters or surrounding whitespace.
   */
  designation?: string;
  description?: string;
  aliases?: Aliases;
  relation:
    | "actuates"
    | "protects"
    | "controls"
    | "measures"
    | "monitors"
    | "feeds"
    | "associated_with";
  from: DeviceRef;
  to: DeviceRef;
  connection?: {
    /**
     * Opaque non-empty identifier with no control characters or surrounding whitespace.
     */
    fromPort: string;
    /**
     * Opaque non-empty identifier with no control characters or surrounding whitespace.
     */
    toPort: string;
    /**
     * Opaque non-empty identifier with no control characters or surrounding whitespace.
     */
    protocol: string;
    medium: "ethernet" | "nrg-bus";
    status?: "planned" | "verified";
    cable?: {
      /**
       * Opaque non-empty identifier with no control characters or surrounding whitespace.
       */
      specification: string;
      lengthM?: number;
      /**
       * Opaque non-empty identifier with no control characters or surrounding whitespace.
       */
      route?: string;
    };
  };
  assembly?: {
    kind: "cable" | "direct" | "cap";
    /**
     * Opaque non-empty identifier with no control characters or surrounding whitespace.
     */
    fromPort: string;
    /**
     * Opaque non-empty identifier with no control characters or surrounding whitespace.
     */
    toPort: string;
    pinMapping: {
      status: "unresolved" | "not-applicable";
      /**
       * Opaque non-empty identifier with no control characters or surrounding whitespace.
       */
      reason: string;
    };
    cable?: {
      /**
       * Opaque non-empty identifier with no control characters or surrounding whitespace.
       */
      specification: string;
      /**
       * Opaque non-empty identifier with no control characters or surrounding whitespace.
       */
      manufacturer?: string;
      /**
       * Opaque non-empty identifier with no control characters or surrounding whitespace.
       */
      orderNumber?: string;
      lengthM?: number;
      /**
       * Opaque non-empty identifier with no control characters or surrounding whitespace.
       */
      route?: string;
    };
    status?: "documented" | "planned" | "verified";
  };
}
export interface DeviceRef {
  /**
   * Opaque non-empty identifier with no control characters or surrounding whitespace.
   */
  device: string;
}
