# Murr 85658 — candidate

Identity: `murr-pilot:85658`, manufacturer field `Murr`, order `85658`. The manufacturer-branded sheet identifies Murrelektronik article 85658, model MDD 050-24/+-10V. The historical 24 V DC input / ±10 V DC output description agrees; no code substitution is involved.

## Evidence

**S1:** [Product-PDF for Article 85658](https://shop.murrelektronik.com/index.php?anid=000000000000085658_1&cl=details&fnc=outputSpecSheet&lang=1), Murrelektronik GmbH, downloaded version **2026-09-13**, physical pp. 1–3. Saved as `references/85658.pdf` with acquisition metadata and extracted text. This is the version printed in the downloaded bytes; the web index returned an older generated date.

Page 1 establishes identity, 24 V DC input, ±10 V DC output, 5 W total and 2 × 0.25 A. Page 2 specifies maximum 0.25 A per output channel, 0–50 °C operation without condensation, short-circuit/overload protection and 4 kV input/output insulation test. Page 3 lists two four-pole screw-terminal groups. Page 1 and page 3 were visually inspected, followed by a 500 DPI diagram crop (40,500,286,670 PDF points).

## Connections and continuity

Eight screw clamps: input two `+` and two `0 V` returns; output `+`, `0`, `0`, `−`. The input table calls its returns `−`; the diagram calls them `0 V`. Aliases distinguish input/output and repeated markings; suffixes identify diagram order within each repeated marking, not engraved terminal numbers or installation orientation. No pin-number mapping from the flattened table is assumed.

The enlarged diagram explicitly joins each repeated input pair before the converter and joins the two output zeros with a line and junction dots. These are exactly the three fixed links in the result. Neither the fuse/diodes nor the converter creates another fixed link. No PE, shield, signal or communication connection appears in the complete electrical diagram and two four-pole inventory; no rail bond is inferred.

## Modeling limits

Input is a load function, bipolar output uses two source functions, and spare input clamps use individual terminal symbols. All eight clamps are rendered. No individual clamp is universally required: input polarity groups have interchangeable duplicates, and output use depends on the application. Group-level supply requirements cannot be expressed by individual `required` flags.

Ratings remain in notes to avoid assigning one voltage across input and bipolar outputs. The sheet labels 0.85 A as “Current input AC” despite its DC input; that label is not propagated. Its output table gives 8.55–10.5 V without explaining conditions; this range is not modeled as nominal intent. The 4 kV value is an insulation test, not a working voltage. No essential connection question remains; manufacturer clarification would be needed before relying on the anomalous current label or output tolerance table.

The exact manufacturer PDF supplied the necessary diagram, so no broader catalog search was needed. The short product URL failed in the web tool; the direct manufacturer PDF opened and downloaded successfully. No builds, installs or bespoke verification were run.
