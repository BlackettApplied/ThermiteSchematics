/**
 * Generated from the canonical Thermite Schematics JSON Schemas.
 * Do not edit this file directly; run bun run generate:types.
 */

/**
 * Alternative names for an entity.
 */
export type Aliases = string[];

export interface Cable {
  /**
   * Lowercase canonical RFC 4122/9562 UUID using version 1 through 8 and the RFC variant.
   */
  uid: string;
  kind: "cable";
  /**
   * Opaque non-empty identifier with no control characters or surrounding whitespace.
   */
  designation: string;
  /**
   * A library-qualified type identifier in <library-name>:<type-name> form.
   */
  type: string;
  description?: string;
  aliases?: Aliases;
  conductors: {
    id: string;
    /**
     * End A and end B. A null endpoint requires usage spare (enforced by structural validation).
     *
     * @minItems 2
     * @maxItems 2
     */
    endpoints: [
      {
        /**
         * Opaque non-empty identifier with no control characters or surrounding whitespace.
         */
        device: string;
        /**
         * Opaque non-empty identifier with no control characters or surrounding whitespace.
         */
        terminal: string;
      } | null,
      {
        /**
         * Opaque non-empty identifier with no control characters or surrounding whitespace.
         */
        device: string;
        /**
         * Opaque non-empty identifier with no control characters or surrounding whitespace.
         */
        terminal: string;
      } | null,
    ];
    usage?: "in-use" | "spare";
  }[];
  /**
   * Opaque non-empty identifier with no control characters or surrounding whitespace.
   */
  fromLocation?: string;
  /**
   * Opaque non-empty identifier with no control characters or surrounding whitespace.
   */
  toLocation?: string;
}
